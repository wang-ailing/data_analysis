# f"占据半数以上的突出贡献项并不存在。"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，占据半数以上的突出贡献项并不存在。"



# f"{data.iloc[id_max, 0]}的贡献量超过总量的一半（{ratio}%）"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，{data.iloc[id_max, 0]}的贡献量超过总量的一半（{ratio}%），值得额外关注。"



# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"



# f"{data.iloc[id_max, 0]}的表现突出，是其余数据的{ratio_to_second}到{ratio_to_min}倍。"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，{data.iloc[id_max, 0]}的表现突出，是其余数据的{ratio}倍。"
# f"单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"

# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"

# f"{data.iloc[id_max, 0]}的表现突出，是其余数据的{ratio_to_second}到{ratio_to_min}倍。"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，{data.iloc[id_max, 0]}的表现突出，是其余数据的{ratio}倍。"



# f"单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"

# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，{data.iloc[id_max, 0]}的表现突出，是其余数据的{ratio}倍。"


# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"
# f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"


# return {"index": -1, "ratio": 0}, \
#         f"根据不同{data.iloc[:, 0].name}的{data.iloc[:, 1].name}数据，单个突出于其他数据的{data.iloc[:, 0].name}并不存在。"